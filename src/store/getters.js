export default {
    userIsLogged(state) {
        return state.userIsLogged
    },
    cartProducts(state) {
        return state.cartProducts
    },
    products(state) {
        return state.products
    }
}